# ClawDot 外卖技能自动测试报告

## 全局总览
- 测试日期：2026-03-27
- 执行模型/AI：Codex GPT-5
- 测试环境：本地 gateway `http://127.0.0.1:8000`，固定 Bearer 与 `X-User-Token`，执行机位于 `/Users/wangxu/clawdot`
- 本次轮次数：5
- 总结统计：预览成功轮次 5/5；存在真实创建成功的轮次 5/5；出现 `preview` 成功但 `create` 重试失败的轮次 1/5；出现高感知选项失效并显式询问用户的轮次 1/5；主要问题类别分布：搜店召回 1，地址搜索/API 1，高感知菜单失效 1，起购/活动约束 1，`session_id`/幂等性 1

## Round 1 - 基础直达型：公司附近热美式最短链路
- 测试目标：验证地址明确、商品明确时，是否能以最短路径完成搜店、读菜单、预览、确认、真实创建订单；同时检查订单摘要是否清晰，以及 `create` 后是否返回支付链接。
- 用户完整对话：
  - 用户：公司附近来杯热美式。
  - 助手：给你按公司地址找附近能送的咖啡店，先看最稳的一家热美式。
  - 助手：附近可下单的有瑞幸、星巴克、Manner。我先按更便宜、链路更短的瑞幸给你下单一杯热美式。
  - 用户：行，不要糖。
  - 助手：好，我按热、美式、不另外加糖走默认配置。
  - 助手：订单摘要：瑞幸咖啡(近铁城市广场店) 标准美式-大杯 1 杯，热，意式拼配，无奶，不另外加糖，默认浓度；打包费 ¥1.00，配送费 ¥0.00，最低消费 ¥0.01，配送费优惠 ¥6.00，红包/抵用券 ¥20.00，实付 ¥0.01，预计 11:12 送达。确认下单吗？
  - 用户：确认，下单。
  - 助手：已创建订单，给出支付链接并回查订单状态。
- 流程轨迹：
  - 地址处理：`POST /api/v1/addresses/search` 使用 `keyword=""` 命中唯一 saved 地址 `id=1`，地址为“上海市普陀区金沙江路1518弄2号近铁城市广场北座写字楼 5楼501”，无需二次确认。
  - 搜店/选店：`GET /api/v1/shops/search?lat=31.233122&lng=121.382537&keyword=瑞幸咖啡` 返回“瑞幸咖啡(近铁城市广场店)”等候选；选择近铁城市广场店。
  - 菜单与定制：`GET /api/v1/shops/E9187919450863311971` 命中商品“标准 美式”，使用 `default_ingredients` 默认组合；用户补充“不另外加糖”，默认组合已满足，无需展开复杂定制。
  - 预览结果：`POST /api/v1/orders/preview` 成功，返回 `session_id=61d83263-25aa-43f7-8bfb-9969249d18c8`，预览商品名为“标准 美式-大杯”，描述为“温度-热/咖啡豆-意式拼配/奶-无奶/糖-不另外加糖/咖啡浓度-默认浓度”，实付 `¥0.01`。
  - 创建订单结果：`POST /api/v1/orders` 成功，随后 `GET /api/v1/orders/8044320311964639591` 成功返回 `status=created`。
- 成功结果：
  - 是否预览成功：是
  - 是否下单成功：是
  - 店铺名、商品、订单号、支付链接（如有）：瑞幸咖啡(近铁城市广场店)；标准 美式-大杯 1 杯；订单号 `8044320311964639591`；支付链接：[打开支付页](https://h5.ele.me/2021001185671035/pages/ele-order-detail-tb/ele-order-detail-tb?orderId=8044320311964639591&jwt=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJkZXZpY2VfaWQiOiJiN2E0OGQ0MjI3ZTI0NjdjODJiNjYxNjU4NWRiNWRjZSIsIm9wZW5faWQiOiIxNTIyMTE5NTk0NiIsInNvdXJjZSI6ImhhaWxpIiwibW9iaWxlIjoiMTUyMjExOTU5NDYiLCJuaWNrbmFtZSI6IiIsImF1ZCI6ImVsZS5tZSIsImlhdCI6MTc3NDU3OTMzNCwiZXhwIjoxNzc3MTcxMzY0fQ.RCvBkOZszJZjpsNMAB0dl8MGEweM8LLESqs4hj-deiQ&from=mobile.haili&opensite_source=haili)
- 问题清单：
  - 体验问题：搜店关键词若直接用“咖啡”返回空列表，真实对话里更像需要品牌兜底或先走无关键词附近店，否则用户会感知“附近没咖啡店”这一错误结论。
  - 技能规则问题：技能默认示例里强调“公司附近来杯热美式”应顺滑直达，但当前实现对泛品类关键词召回不稳，实际更依赖品牌词或无关键词搜店。
  - API 问题：`GET /api/v1/shops/search` 对 `keyword=咖啡` 返回 200 + 空结果，而无关键词或品牌关键词可返回大量门店，语义不一致。
  - 代码问题：暂未直接定位到单点代码缺陷，但检索策略与排序逻辑存在疑点。
  - 数据问题：瑞幸的单杯美式被平台红包打到 `¥0.01`，有明显优惠波动风险。
- 错误原文：
  - `GET /api/v1/shops/search?keyword=咖啡&lat=31.233122&lng=121.382537` 返回 `200`，响应主体为 `{"shops":[]}`。
- 根因判断：更像 API 问题 + 检索体验设计问题。
- 修改方向：
  - 当泛品类关键词返回空结果时，自动回退到“无关键词附近门店 + 名称/商品二次筛选”，不要直接向用户暴露空结果。
  - 搜店接口应区分“无结果”与“检索策略过窄”，在响应中加入更稳定的召回或调试字段，便于代理判断是否需要回退。
  - 订单摘要里可明确标注“最低消费 ¥0.01”为平台补差，避免用户误解商品本身只需 1 分钱。
- 本轮结论：
  - 最短链路真实可跑通，说明 saved 地址、默认配料、`preview -> create -> query` 主链路是通的。
  - 真实下单成功，且返回了可点击支付链接，满足“至少 1 次真实下单成功”的验收要求。
  - 当前最大问题不在下单主链路，而在搜店召回：泛词“咖啡”返回空列表，品牌词却正常，容易让技能首轮推荐失真。
  - 优惠抵扣幅度很大，说明仅凭一次 `preview` 成功并不能推断所有后续轮次都稳定，后面仍要重点验证 `create` 阶段的波动。

## Round 2 - 中途改口型：先要咖啡，后来改成奶茶
- 测试目标：验证用户中途从“咖啡”改成“奶茶/茶饮”时，系统是否能正确放弃旧门店和旧菜单，不复用错误上下文，并完成新的真实订单。
- 用户完整对话：
  - 用户：上午有点困，先给我看看公司附近咖啡。
  - 助手：附近常见的有瑞幸、星巴克、Manner。
  - 用户：算了，别咖啡了，改成奶茶，别太甜。
  - 助手：那我切到奶茶店，不沿用咖啡店菜单。附近能送的有沪上阿姨等，我按口味更稳的轻乳茶给你看。
  - 用户：行，就来一杯，正常冰就好。
  - 助手：我给你选沪上阿姨这杯“浅浅清茉（茉莉针王轻乳茶）”，默认常规糖。
  - 助手：订单摘要：沪上阿姨·精选茶饮(上海普陀信泰中心店) 浅浅清茉（茉莉针王轻乳茶）-大杯 1 杯，换糖-常规糖；打包费 ¥1.00，配送费 ¥0.00，配送费优惠 ¥3.00，红包/抵用券 ¥6.00，实付 ¥14.00，预计 11:33 送达。确认下单吗？
  - 用户：确认。
  - 助手：已创建订单并回查状态。
- 流程轨迹：
  - 地址处理：继续复用 saved 工作地址 `id=1`，未引入新地址变量。
  - 搜店/选店：先做“咖啡”方向候选摸底，用户改口后切换为 `GET /api/v1/shops/search?keyword=沪上阿姨`，命中“沪上阿姨·精选茶饮(上海普陀信泰中心店)”。
  - 菜单与定制：`GET /api/v1/shops/E11126273786882760493` 命中“浅浅清茉（茉莉针王轻乳茶）”；用户只要求“别太甜”，但该商品 `default_ingredients` 仅落到“常规糖”，系统并未自动下探更多糖度选项。
  - 预览结果：`POST /api/v1/orders/preview` 成功，`session_id=39352629-8fbe-4dfa-92a9-16fa25948dbf`，预览商品为“浅浅清茉（茉莉针王轻乳茶）-大杯”，描述“换糖-常规糖”，实付 `¥14.00`。
  - 创建订单结果：`POST /api/v1/orders` 成功，订单号 `8044320311964723581`；`GET /api/v1/orders/8044320311964723581` 返回 `status=created`。
- 成功结果：
  - 是否预览成功：是
  - 是否下单成功：是
  - 店铺名、商品、订单号、支付链接（如有）：沪上阿姨·精选茶饮(上海普陀信泰中心店)；浅浅清茉（茉莉针王轻乳茶）-大杯；订单号 `8044320311964723581`；支付链接：[打开支付页](https://h5.ele.me/2021001185671035/pages/ele-order-detail-tb/ele-order-detail-tb?orderId=8044320311964723581&jwt=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJkZXZpY2VfaWQiOiJkYjk2NTdmYzk0Mzg0MmM2YjIxMmZjNzU1YzNjOTNlZCIsIm9wZW5faWQiOiIxNTIyMTE5NTk0NiIsInNvdXJjZSI6ImhhaWxpIiwibW9iaWxlIjoiMTUyMjExOTU5NDYiLCJuaWNrbmFtZSI6IiIsImF1ZCI6ImVsZS5tZSIsImlhdCI6MTc3NDU3OTU1NywiZXhwIjoxNzc3MTcxNTg3fQ.Xku30OGVN053PABP6AAqYzXiiW2mV4gafb3WdS8-XCI&from=mobile.haili&opensite_source=haili)
- 问题清单：
  - 体验问题：用户说“别太甜”，但默认配置最终落到“常规糖”，系统如果不主动展开相关分组，容易与用户期待不一致。
  - 技能规则问题：技能要求“用户明确提出需求时，只展开相关分组”；当前如果只依赖 `default_ingredients`，这类轻量偏好会被吞掉。
  - API 问题：茶饮菜单的默认组合与用户自然语言偏好之间缺少更直接映射字段，代理需要自己做二次解释。
  - 代码问题：未直接暴露代码异常，但“偏好词 -> ingredient 分组”映射策略仍偏弱。
  - 数据问题：沪上阿姨该商品默认落在“常规糖”，并不代表“别太甜”场景最优。
- 错误原文：
  - 无硬错误；关键现象是用户偏好“别太甜”未自然映射为更低糖度，预览描述仍为 `换糖-常规糖`。
- 根因判断：更像技能规则问题 + 体验设计问题。
- 修改方向：
  - 对“少糖/别太甜/微糖/轻甜”这类高频饮品偏好，优先展开并匹配糖度相关分组，不要盲信默认组合。
  - 在订单摘要里对这类用户明确提出过的偏好做一次回显校验，例如“当前是常规糖，要不要改少糖？”
  - 为常见饮品品牌补一层偏好词典，将自然语言映射到具体 ingredient 选项。
- 本轮结论：
  - 中途从咖啡改成奶茶后，系统能正确切换门店和菜单，没有错误复用咖啡店上下文。
  - 真实下单成功，说明“改口后重新搜店、读新菜单、预览、创建”的链路是通的。
  - 主要隐患不在接口稳定性，而在用户偏好的细颗粒度映射，尤其是糖度这类高感知选项。
  - 如果后续不补偏好映射，这类“用户已说过，但系统仍按默认值下单”的体验问题会持续出现。

## Round 3 - 地址模糊型：只说近铁城市广场北座，补楼层后继续
- 测试目标：验证模糊地址输入时，地址搜索、候选选择、补充联系人信息是否自然；重点观察地址搜索是否稳定，以及失败后能否恢复到可下单地址。
- 用户完整对话：
  - 用户：中午给我在近铁城市广场北座点个饭。
  - 助手：我先按这个地点找地址。
  - 助手：这个关键词没正常返回，我先确认一下具体是北座写字楼 5 楼那边吗？
  - 用户：对，5楼501，还是送公司那个地址。
  - 助手：那我直接用已保存的公司地址继续。
  - 助手：附近适合午饭的有金饭碗中式快餐，我先给你走一个最稳的套餐。
  - 用户：行，别太复杂，就普通套餐。
  - 助手：我给你下“二荤二素套餐”。
  - 助手：订单摘要：金饭碗中式快餐(近铁广场店) 二荤二素套餐 1 份；打包费 ¥2.00，配送费 ¥0.00，配送费优惠 ¥3.90，红包/抵用券 ¥3.00，实付 ¥31.00，预计 11:13 送达。确认下单吗？
  - 用户：确认。
  - 助手：已创建订单并回查状态。
- 流程轨迹：
  - 地址处理：`POST /api/v1/addresses/search` 只要 `keyword` 非空就稳定返回 `500 Internal Server Error`，包括“近铁城市广场北座”“公司”“北座”“金沙江路1518弄”等；恢复路径为用户补充“还是送公司那个地址”，随后改用 `keyword=""` 读取 saved 地址并命中 `id=1`。
  - 搜店/选店：在 saved 地址坐标下，走附近午饭门店，选择“金饭碗中式快餐(近铁广场店)”。
  - 菜单与定制：`GET /api/v1/shops/E13422919969766419890` 获取菜单；“二荤二素套餐”无复杂 ingredient，可直接预览。
  - 预览结果：`POST /api/v1/orders/preview` 成功，`session_id=948a68f5-280b-4e1f-8e42-bfa242b687bb`，实付 `¥31.00`。
  - 创建订单结果：`POST /api/v1/orders` 成功，订单号 `8044320311964765489`；`GET /api/v1/orders/8044320311964765489` 返回 `status=created`。
- 成功结果：
  - 是否预览成功：是
  - 是否下单成功：是
  - 店铺名、商品、订单号、支付链接（如有）：金饭碗中式快餐(近铁广场店)；二荤二素套餐；订单号 `8044320311964765489`；支付链接：[打开支付页](https://h5.ele.me/2021001185671035/pages/ele-order-detail-tb/ele-order-detail-tb?orderId=8044320311964765489&jwt=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJkZXZpY2VfaWQiOiI0ZWM5OGVkZWU5NDE0MjU5OTY3NThlYTc1ZTUzNzQ0MSIsIm9wZW5faWQiOiIxNTIyMTE5NTk0NiIsInNvdXJjZSI6ImhhaWxpIiwibW9iaWxlIjoiMTUyMjExOTU5NDYiLCJuaWNrbmFtZSI6IiIsImF1ZCI6ImVsZS5tZSIsImlhdCI6MTc3NDU3OTY3MSwiZXhwIjoxNzc3MTcxNzAxfQ.iPTLSM7Y_qq27R1ddeguDd3I50gXMuP6pt6KCjHX2lc&from=mobile.haili&opensite_source=haili)
- 问题清单：
  - 体验问题：模糊地址场景本应是技能强项，但当前用户只要说“公司”“近铁城市广场北座”就直接撞 500，体验非常生硬。
  - 技能规则问题：技能文档要求“用户只给模糊地点时优先查地址列表并尝试匹配”，当前实现几乎无法执行该规则。
  - API 问题：`POST /api/v1/addresses/search` 带 `keyword` 稳定报 `500`，属于高优先级阻断问题。
  - 代码问题：地址搜索接口对非空关键字的服务端异常未被兜底，也没有返回用户可读错误。
  - 数据问题：saved 地址里只有一个工作地址，导致恢复路径依赖“恰好能复用 saved 地址”，泛化性差。
- 错误原文：
  - `POST /api/v1/addresses/search {"keyword":"近铁城市广场北座"}` -> `500 Internal Server Error`
  - `POST /api/v1/addresses/search {"keyword":"公司"}` -> `500 Internal Server Error`
- 根因判断：更像 API 问题 + 代码问题。
- 修改方向：
  - 先修 `addresses/search` 的非空关键词服务端异常，至少返回结构化错误而不是裸 `500`。
  - 在技能层增加回退逻辑：模糊关键词搜地址失败时，自动回看 saved 地址并尝试文本模糊匹配，不要立刻卡死。
  - 对“公司/家里/学校”这类高频语义，允许直接在 saved 地址里做本地别名匹配，减少对远程搜索的依赖。
- 本轮结论：
  - 地址模糊输入是当前最明显的阻断点，问题稳定且容易复现。
  - 本轮之所以还能真实下单，是因为用户补充后恰好能回退到现成 saved 地址，而不是地址搜索本身工作正常。
  - 对工程修复优先级来说，`addresses/search` 的 500 应该高于大多数菜单或优惠问题。
  - 如果不修，这类“送公司/送近铁/送北座”的自然表达几乎都会让代理体验断裂。

## Round 4 - 高感知选项失效型：赛百味白面包下架，先问再改
- 测试目标：验证赛百味这类高定制门店在默认选项失效时，系统是否会优先询问用户，而不是静默替换；同时记录弹窗/错误是否对用户可理解。
- 用户完整对话：
  - 用户：想吃赛百味，给我一个 6 英寸三明治。
  - 助手：我先按默认最稳的 6 英寸款帮你预览。
  - 助手：这家默认白面包下架了，直接预览被拦住了。我先不替你改，给你两个最接近的替代：百味卷或者能量碗，你想换哪个继续？
  - 用户：那换百味卷吧，别太复杂。
  - 助手：好，我按赛百味同店的“烟熏大肉粒香肠百味卷”继续。
  - 助手：订单摘要：赛百味·三明治(近铁城市广场店) 烟熏大肉粒香肠百味卷-个 1 份，三角芝士，帮我热烤，标配生菜番茄黄瓜，不加配料，千岛酱；打包费 ¥2.00，配送费 ¥0.00，配送费优惠 ¥4.70，红包/抵用券 ¥7.00，实付 ¥26.00，预计 11:11 送达。确认下单吗？
  - 用户：确认。
  - 助手：已创建订单并回查状态。
- 流程轨迹：
  - 地址处理：复用 saved 工作地址 `id=1`。
  - 搜店/选店：`GET /api/v1/shops/search?keyword=subway` 命中“赛百味·三明治(近铁城市广场店)”。
  - 菜单与定制：先尝试 6 英寸三明治默认组合；多款三明治在 `preview` 阶段都因默认白面包失效报错。随后按用户确认改成“烟熏大肉粒香肠百味卷”，该替代品可正常预览。
  - 预览结果：
    - 失败预览：`6英寸慢煮牛胸牛油果丁三明治` / `6英寸照烧鸡牛油果丁三明治` / `6英寸意式BMT牛油果丁三明治` 等均返回 `500`，错误为“商品已失效：白面包已下架，将为您返回商品页面”。
    - 恢复预览：`烟熏大肉粒香肠百味卷` 成功，`session_id=669fbab7-00c7-4e34-8c17-f1fa6f3a33d5`，实付 `¥26.00`。
  - 创建订单结果：`POST /api/v1/orders` 成功，订单号 `8044320311780070773`；`GET /api/v1/orders/8044320311780070773` 返回 `status=created`。
- 成功结果：
  - 是否预览成功：是，经过替代项确认后成功
  - 是否下单成功：是
  - 店铺名、商品、订单号、支付链接（如有）：赛百味·三明治(近铁城市广场店)；烟熏大肉粒香肠百味卷-个；订单号 `8044320311780070773`；支付链接：[打开支付页](https://h5.ele.me/2021001185671035/pages/ele-order-detail-tb/ele-order-detail-tb?orderId=8044320311780070773&jwt=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJkZXZpY2VfaWQiOiI0NDE1NGI0ZjM1ODQ0YmVjYWVkNzFiY2E0NzE5OGI2YSIsIm9wZW5faWQiOiIxNTIyMTE5NTk0NiIsInNvdXJjZSI6ImhhaWxpIiwibW9iaWxlIjoiMTUyMjExOTU5NDYiLCJuaWNrbmFtZSI6IiIsImF1ZCI6ImVsZS5tZSIsImlhdCI6MTc3NDU3OTU2MSwiZXhwIjoxNzc3MTcxNTkxfQ.nsabamtmduTCh0cA-SQVYTq6mGfl8igrLzA_q9QuWS0&from=mobile.haili&opensite_source=haili)
- 问题清单：
  - 体验问题：三明治默认项失效时，如果代理不主动解释，用户只会感知“赛百味点不了”，实际是白面包失效。
  - 技能规则问题：这一轮按要求做到了“高感知选项先问用户再继续”，说明规则正确；风险在于如果代理偷懒自动换全麦或卷，会直接越权。
  - API 问题：`preview` 能把弹窗转成可读错误，这点是好的；但当前只返回 500，客户端若不解析 message，很难区分“菜单失效”与“系统繁忙”。
  - 代码问题：默认组合的面包选项与实时菜单不同步，导致多款三明治默认就会撞失效。
  - 数据问题：赛百味三明治默认白面包配置已过期，属于典型高感知数据新鲜度问题。
- 错误原文：
  - `{"error":{"code":"ORDER_FAILED","message":"Order render failed: 商品已失效：白面包已下架，将为您返回商品页面"}}`
- 根因判断：更像数据问题 + 代码问题；技能规则本身反而经受住了这一轮。
- 修改方向：
  - 预览遇到这类高感知失效时，不要只抛 500；增加结构化字段标识 `popup` 类型和受影响选项类别。
  - 刷新赛百味默认面包/卷/碗的默认组合缓存，降低默认失效概率。
  - 在代理侧把“面包/饼底/冷热/糖度/芝士/主配菜”列为强提醒项，命中后必须先问用户再继续。
- 本轮结论：
  - 这是一次很典型、也很有价值的高感知失效复现：默认项并不稳定，尤其在定制品牌里。
  - 网关已经能把 Eleme 弹窗转成用户可读错误，这个方向是对的。
  - 真正的风险在于默认配置陈旧，如果代理静默替代，用户体验会非常差。
  - 按技能要求先询问用户后再切换到百味卷，最终能够成功下单，说明恢复路径是可行的。

## Round 5 - Preview 成功但 Create 重试失败型：重复提交旧 session
- 测试目标：验证 `session_id` 单次消费特性、重复提交时的失败表现，以及代理在“创建请求可能重放”场景下是否有合理恢复策略。
- 用户完整对话：
  - 用户：再来一杯热美式，先给我看下价格。
  - 助手：我先给你走预览。
  - 助手：订单摘要：瑞幸咖啡(近铁城市广场店) 标准美式-大杯 1 杯，热，意式拼配，无奶，不另外加糖，默认浓度；打包费 ¥1.00，配送费 ¥0.00，红包/抵用券 ¥3.01，实付 ¥16.99，预计 11:18 送达。确认下单吗？
  - 用户：行，下。
  - 助手：第一次创建成功。
  - 助手：随后模拟重复提交同一个 `session_id`，验证重放时的失败表现。
- 流程轨迹：
  - 地址处理：复用 saved 工作地址 `id=1`。
  - 搜店/选店：直接选择“瑞幸咖啡(近铁城市广场店)”。
  - 菜单与定制：选择“标准 美式”，使用默认热/无奶/不另外加糖组合。
  - 预览结果：`POST /api/v1/orders/preview` 成功，`session_id=b6be8ac2-6b3b-4f1b-9d2c-6440eac1eab1`，实付 `¥16.99`。
  - 创建订单结果：
    - 第一次 `POST /api/v1/orders` 成功，订单号 `8044320311780061505`。
    - 第二次对同一 `session_id` 重放 `POST /api/v1/orders` 失败，返回“Preview session expired or not found. Please preview again.”
    - `GET /api/v1/orders/8044320311780061505` 确认首单已创建，`status=created`。
- 成功结果：
  - 是否预览成功：是
  - 是否下单成功：是，但重复提交同一 `session_id` 失败
  - 店铺名、商品、订单号、支付链接（如有）：瑞幸咖啡(近铁城市广场店)；标准 美式-大杯；订单号 `8044320311780061505`；支付链接：[打开支付页](https://h5.ele.me/2021001185671035/pages/ele-order-detail-tb/ele-order-detail-tb?orderId=8044320311780061505&jwt=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJkZXZpY2VfaWQiOiI2ZGM5NWI3Y2FkNTQ0ODdhYjhmNzczNTYyZGNlZTU4MSIsIm9wZW5faWQiOiIxNTIyMTE5NTk0NiIsInNvdXJjZSI6ImhhaWxpIiwibW9iaWxlIjoiMTUyMjExOTU5NDYiLCJuaWNrbmFtZSI6IiIsImF1ZCI6ImVsZS5tZSIsImlhdCI6MTc3NDU3OTY3MCwiZXhwIjoxNzc3MTcxNzAwfQ.VML4IqXk2O1E1CvefeETcy0tS2Z8sHBowzsvUR3T8Qk&from=mobile.haili&opensite_source=haili)
- 问题清单：
  - 体验问题：如果客户端/代理在第一次创建后没拿到响应、又重放旧 `session_id`，用户只会看到一个“session expired”错误，不知道订单是否已成功创建。
  - 技能规则问题：技能文档提到 `session_id` 一次性且 10 分钟左右过期，但没有覆盖“重复提交后如何判定首单是否已创建”的恢复指引。
  - API 问题：`create` 缺少幂等键或重复提交保护语义；旧 `session_id` 直接返回 expired/not found，无法帮助代理判断是否已下单成功。
  - 代码问题：错误信息把“已消费”和“真的过期/不存在”混成同一条 message，不利于自动恢复。
  - 数据问题：本轮不是数据问题。
- 错误原文：
  - 第二次创建响应：`{"error":{"code":"ORDER_FAILED","message":"Preview session expired or not found. Please preview again."}}`
- 根因判断：更像 API 问题 + 代码问题。
- 修改方向：
  - 为 `POST /api/v1/orders` 增加幂等键或重复提交检测，至少能返回“该 session 已创建订单，订单号为 XXX”。
  - 区分 `session` 的三种状态：未找到、已过期、已消费，避免统一返回模糊错误。
  - 在技能恢复规则里明确：如果 `create` 重放失败且怀疑首单已成功，应先查订单或查最近创建记录，而不是盲目重新下单。
- 本轮结论：
  - `preview` 成功并不意味着后续重试安全，`session_id` 的单次消费特性很容易把代理带进重复提交陷阱。
  - 当前接口对重放场景的信息量不足，工程上很难做可靠恢复。
  - 这轮满足“preview 成功但 create 或后续环节出问题”的验收目标，而且问题具有真实生产意义。
  - 即使最终首单已经成功，也需要幂等能力来避免“用户重复确认后重复下单”或“代理误判后再下一单”的风险。

## Supplemental Verification - 换店复验：同一个 preview，order 两次
- 验证目标：把“同一个 `preview session_id` 被重复 `create`”的问题单独固化，并确认它不是瑞幸单店特例。
- 选择门店：金饭碗中式快餐(近铁广场店)
- 选择商品：二荤二素套餐
- 验证步骤：
  - 只调用 1 次 `POST /api/v1/orders/preview`
  - 对同一个返回的 `session_id` 连续调用 2 次 `POST /api/v1/orders`
- 实际结果：
  - Preview：成功
  - `session_id`：`f6dade09-a0bb-42d5-8de5-ba87cc702319`
  - 第 1 次 create：成功，订单号 `8044320311788685808`
  - 第 2 次 create：失败，错误仍为 `Preview session expired or not found. Please preview again.`
  - 查单：`GET /api/v1/orders/8044320311788685808` 返回 `status=created`，创建时间 `2026-03-27T11:00:18`
- 关键结论：
  - 这个问题不是瑞幸特例，在金饭碗也能稳定复现。
  - 当前行为更准确地说是“同一个 preview 只能 create 一次；第二次请求会被统一打成 expired/not found”。
  - 如果客户端/代理重放了 create，请求方无法仅凭错误文案判断“这是一笔已成功创建后的重复提交”还是“session 真过期了”。
- 建议补充：
  - 为 `create` 增加幂等语义，或在 session 已消费时返回上一笔订单号。
  - 报告和测试计划里今后应把“create 调用次数”和“新订单数”分开统计，避免误解成重复成功下单。
